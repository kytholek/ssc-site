# Dependencies
node_modules/

# Local Netlify folder
.netlify

# Wrangler local state
.wrangler/

# Environment files
.env
.env.local
.dev.vars

# Keep package.json local only — Pages must not see it (triggers npm install)
package.json

# Keep wrangler.toml local only — Pages CI treats it as a Worker deployment config
wrangler.toml
